import { Button, Icon, Input, palette } from '@my/ui-kit'
import { Tooltip } from 'antd'
import { _history } from 'App'

export const HomePage = () => {

  return (
    <>
      <p>homePage</p>
    </>
  )
}
